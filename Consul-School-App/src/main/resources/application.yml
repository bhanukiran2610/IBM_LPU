server:
  port: 8000
spring:
  boot:
    admin:
      client:
        url: http://localhost:9999
  application:
    name: school-service
  cloud:
    consul:
      discovery:
        register: true
        service-name: school-service
      host: localhost
      port: 8500